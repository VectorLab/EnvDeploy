processManagement:
   pidFilePath: ${PWD}/run/pid
   timeZoneInfo: ${PWD}/tz
net:
   bindIp: 0.0.0.0
   port: 27017
systemLog:
   destination: file
   path: ${PWD}/run/log
   logAppend: true
storage:
   dbPath: ${PWD}/data
   directoryPerDB: true
security:
   authorization: disabled
